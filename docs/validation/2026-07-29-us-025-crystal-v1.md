# Validation Report: US-025 Crystal v1

Date: 2026-07-29

Status: **Rejected and superseded.** Independent review found controlled-state,
accessibility fallback, text semantics, date-boundary, customization, and
migration defects. Replacement evidence is recorded in
`docs/validation/2026-07-30-us-025-review-remediation.md`.

## Scope

Validated the breaking typed Liquid Glass v1 public contract, shared Dart and
Swift platform-view lifecycle, deterministic bridge-traffic rules, Material
fallbacks, migrated example, and iOS 26 SDK compilation.

## Commands Run

```text
dart format --output=none --set-exit-if-changed lib test example/lib example/test
flutter analyze
flutter test
cd example && flutter test
cd example && flutter build ios --simulator --debug
xcodebuild -version
xcrun --sdk iphonesimulator --show-sdk-version
xcrun swift --version
flutter pub publish --dry-run
git diff --check
```

Harness matrix, intake, decision, tool-capability, story verification, and
detailed trace commands use the absolute binary at
`/Users/winterzxzz/Documents/Local/AppMobiles/flutter_native_view/scripts/bin/harness-cli`
with the Crystal worktree as the current directory.

## Results

| Check | Result | Notes |
| --- | --- | --- |
| Format | pass | 57 Dart files checked; 0 required changes before the final focused fixes, and changed files were formatted afterward |
| Analyze | pass | `No issues found` |
| Unit/widget | pass | 16/16 root tests |
| Example regression | pass | 68/68 tests |
| Platform | pass | iOS Simulator debug build produced `example/build/ios/iphonesimulator/Runner.app` |
| Toolchain | observed | Xcode 26.3 (17C529), iOS Simulator SDK 26.2, Swift 6.2.4 |
| Package archive | qualified | 77 KB compressed; generated build, Pods, ephemeral, and Harness artifacts excluded |
| Whitespace | pass | `git diff --check` clean at evidence capture |
| Harness story verification | pass | `US-025` reran its configured `flutter analyze && flutter test` command |

The publish dry-run exits 65 with two Git-state warnings: tracked pre-v1 files
are deleted but cannot be removed from the index until a commit, and the
working tree is intentionally modified. This task explicitly prohibits a
commit. The archive listing and size were produced successfully before those
warnings, and no generated build or Harness database content was present.

## Deterministic Performance Evidence

`test/config_tracker_test.dart` proves the locally measurable traffic rules:

- a structurally equal nested snapshot causes zero outbound update decisions;
- two same-frame scheduling requests enqueue one flush;
- accepting a native value patch prevents the controlled rebuild from echoing
  that same value;
- snapshots are defensively frozen against later caller mutation;
- the exact creation snapshot remains the comparison base, so a newer config
  is sent after asynchronous platform-view creation.

`LiquidGlassDiagnostics` records only view type and operation kind. The tests
also prove that non-iOS fallbacks generate zero channel operations.

## Security And Boundary Review

- Swift receives codec values through `GlassArguments`; individual models use
  typed reads, defaults, and range clamps before reaching SwiftUI controls.
- Unknown Dart-to-native methods return `FlutterMethodNotImplemented`.
- Channel handlers are cleared and hosted views detached during teardown.
- Diagnostics retain no label, text, color, or argument payload.
- A focused secret-pattern sweep found no credential material in the changed
  surface. No registered Harness security-scan provider was present, so this
  is not evidence of a formal dependency or vulnerability scan.

## Gaps

- No registered Harness provider exists for impact analysis, coverage,
  security scanning, performance benchmarking, or documentation lookup.
- No Instruments run was performed. Frame time, GPU cost, memory, energy, and
  large-list behavior remain unmeasured.
- The iOS build proves availability and Swift compilation, not visual fidelity,
  VoiceOver behavior, Reduce Transparency/Reduce Motion appearance, or touch
  behavior on an iOS 26 runtime.
- No interactive end-to-end simulator session was run; E2E proof remains
  explicitly unset in the durable matrix.
